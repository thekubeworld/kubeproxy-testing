name: "foo"
on:
  workflow_dispatch:
  schedule:
    # Daily, 3:00pm
    - cron: '00 15 * * *'

jobs:
  iptables-kubeproxy-clusterip-cni-kindnetd:
    runs-on: [ ubuntu-18.04 ]
    steps:
      - uses: actions/checkout@v2
        with:
           path: main

        # Setup Go
      - name: Setup Go
        uses: actions/setup-go@v2
        with:
          go-version: '1.16.3' # The Go version to download (if necessary) and use.
      - uses: actions/checkout@master
        with:
          repository: thekubeworld/k8s-local-dev
          path: k8s-local-dev
      - run: |
          pushd ./k8s-local-dev
            ./k8s-local-dev kindnetd
          popd
          pushd ./main
            mkdir -p downloads
            GO111MODULE=off go get github.com/moby/spdystream
            GO111MODULE=off go get github.com/thekubeworld/k8devel/pkg/client 
            GO111MODULE=off go get github.com/gookit/color
            GO111MODULE=off go get github.com/sirupsen/logrus
            GO111MODULE=off go run ./tests/clusterip/main.go &> downloads/output_kubeproxy_iptables_clusterip.log
          popd
      - uses: actions/upload-artifact@v2
        with:
          name: logs
          path: ./main/downloads/
          retention-days: 60
